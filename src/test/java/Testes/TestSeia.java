package Testes;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import java.util.concurrent.TimeUnit;

/*public class TestSeia {
    //Abrindo navegador
        System.setProperty("webdrive.chrome.driver", "C:/Users/Yasmin/IdeaProjects/Cursinho");
    WebDriver site = new ChromeDriver();
        site.manage().timeouts().implicitlyWait(5,TimeUnit.SECONDS);
    // Navegando para a pagina
        site.get("http://10.90.2.65/");
        return site;
}
*/